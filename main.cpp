#include "Game.h"

int main(int argc, char * argv[])
{
  Game game(argc, argv);
   game.run();
  return 0;
  // Dictionary dict;
  // dict.load("plik.txt");
  // dict.checkWord("ktury"); //false
  // dict.checkWord("ser") //true
}
